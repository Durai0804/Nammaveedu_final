version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: nv
      POSTGRES_PASSWORD: nvpass
      POSTGRES_DB: nammaveedu
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
